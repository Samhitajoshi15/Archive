function reverseString(s) {
    try
    {
        if(!s.split){
        console.log("s.split is not a function");
        console.log(s);
        }
        else
        console.log(s.split("").reverse().join(""));
    }
    catch(err){
        console.log("s.split is not a function")
    }
}
